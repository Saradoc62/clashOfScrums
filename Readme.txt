#Compile project

>> mkdir build
>> cd build
>> cmake ..
>> make
>> make test

Requires Boost and Qt4 libraries

#run project

>> cd bin
>> ./clashOfScrums -s (--shell) for shell mode
>> ./clashOfScrums -g (--gui) for Qt GUI mode
